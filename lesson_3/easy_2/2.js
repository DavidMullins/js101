let numbers = [1, 2, 3, 4, 5];

console.log(numbers.slice(0, 5).reverse());
console.log(numbers);

/*
console.log(numbers.sort(...numbers));
console.log(numbers);
*/

// Need to worked on this spread stuff. Completely didn't follow it.