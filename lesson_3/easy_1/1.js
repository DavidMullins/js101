/*
No, I think it will set numbers[x], where x is 3, 4, and 5, to undefined and
then set numbers[6] to 5.
(Turns out this leaves them empty, but it returns undeefined)

BONUS: numbers[4] will return undefined.
*/